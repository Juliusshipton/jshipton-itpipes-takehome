{
  "name": "legacy-file-conversion",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:legacy": "vitest run tests/legacy.spec.ts --reporter=verbose"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^5.0.0"
  }
}
